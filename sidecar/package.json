{
  "name": "minepanel-sidecar",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target node --format esm",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "dockerode": "^4.0.4",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/dockerode": "^3.3.34",
    "@types/ws": "^8.5.14",
    "typescript": "^5.8.2"
  }
}
